# Автономный прогон — 2026-06-16

Журнал автономной сессии. Задачи (от пользователя):
1. Bug #1 — линии E-поля странно клипаются/прерываются.
2. Bug #2 — вода-конденсатор: гранулы внутри отвязаны от системы частиц (должны
   подходить прямо к мембране, быть частью каналов).
3. Bug #3 — конденсатор в механике плохо себя ведёт.
4. Research — масштабный рекурсивный ресёрч магнетизма + новых модулей (турнир
   кандидатов: ресёрч → фильтр → ресёрч отфильтрованных → ...).

Режим: автономно, без остановок на подтверждения. Заторы — помечаю и иду дальше.

---

## Лог прогресса

### Старт
- Подняты задачи #1–#4, создан журнал.
- Порядок: сначала 3 бага (конкретны, верифицируемы), затем многораундовый ресёрч.

### Bug #1 — линии E-поля (улучшено)
- Диагноз по снимку (--layers electricField,lic): LIC-текстура поля обрывалась жёстким
  прямоугольным краем (bbox источников ± falloff, в приложении ещё и clamp к viewMin/Max);
  часть стримлайнов обрывалась в воздухе (лимит 84 шага).
- Фикс: растушёвка альфы LIC к краям прямоугольника (`emitLICField`, edgeFade по бортику
  1/8) + длиннее трассировка линий (84→150 шагов, reserve 160).
- ЗАТОР/ограничение: интерактивно в приложении проверить не могу (снимок — фикс. камера
  ±1e6, в приложении viewMin/Max = видимый прямоугольник). Возможный остаток — обрезка
  на краю вьюпорта при пане/зуме (это ожидаемо). На снимке край LIC мягче, линии непрерывны.

<!-- далее дописываю по ходу: что сделано, заторы, решения -->
