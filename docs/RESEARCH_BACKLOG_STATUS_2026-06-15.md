# Статус ресёрч-бэклога на 2026-06-15 (сверено с origin/main)

> **Зачем этот файл.** `docs/RESEARCH_REVIEW_2026-06-14.md` и `docs/research/*`
> писались ДО волны фиксов в ветке `fix/research-p0p1` (влита в main). Поэтому
> МНОГИЕ находки там уже исправлены, а сам обзор вводит в заблуждение. Этот файл —
> «оверлей» актуального статуса каждой находки, сверенный чтением кода `origin/main`.
>
> **ПРАВИЛО:** прежде чем брать задачу из RESEARCH_REVIEW — проверь её статус здесь
> и в `origin/main` (`git show origin/main:<файл>` / `git grep ... origin/main`).
> НЕ сверяй с веткой `claude/analysis` — она отстаёт от main.

## ✅ УЖЕ СДЕЛАНО в main (НЕ делать заново) — с доказательством в коде

| Находка | Что и где |
|---|---|
| V8 — дрейф ∝\|I\|, ноль при I=0 | `src/physics/DriftModel.h` (`kDriftVisualScale`, без аддитивного пола) |
| V9 — поверхностный заряд ∝ \|dV\|/L | `src/physics/SurfaceChargeModel.h` (`gradientMag=\|dV\|/lenSafe`, `gradScale`, `kRefWireLength`) |
| B5 — перцептивная палитра viridis | `src/render/ColorMaps.h` (`kViridisLut` 17 анкеров, `kMagmaLut`, `enum class Colormap`, `colormapSample`) |
| V6 — диод с порогом (НЕ нулевой) | `src/solver/CircuitSolver.cpp` `kDiodeForwardDrop = 0.7` (PWL-companion, переключение по `voltageDrop>0.7`) |
| V3 — конденсатор хранит Q, а не V | `src/solver/CircuitSolver.cpp` `state.capCharge[id] = C·V`, чтение `capCharge/C` |
| V4 — инвалидация trapezoidal-истории на событиях | `src/simulation/LiveSim.cpp` `onCircuitEvent → m_state.invalidateHistory()` |
| V10 — соленоид + Био–Савар отрезка + спад 1/r | `src/physics/SolenoidModel.h` (внутри однородное, снаружи диполь 1/r³); `src/physics/MagneticFieldModel.h` `biotSavartFiniteSegment` + «5 радиусов с видимым спадом ~1/r» |
| V13 — HiDPI: uiScale → screen-space примитивы | влито из p0p1 |
| V15 — heat-glow по температуре, а не мощности | влито из p0p1 (виден остаточный glow при I=0) |
| V14 — частицы: радиус при смене толщины, нет застревания при I=0, LCG старшие биты | влито из p0p1 |
| V11/V12 — механика: пружина∝q, тормоз∝скорость, семантика турбины | влито из p0p1 |
| µF — битый ключ единицы в I18n | починен 2026-06-15 (`"\xC2\xB5" "F"`), тест `I18n.MicroFaradUnitKeyTranslates` |

## 🟡 ЧАСТИЧНО — есть основа, но не закрыто

| Находка | Что есть / чего нет |
|---|---|
| A1 — валидация цепи | `src/circuit/CircuitValidator.h` ЕСТЬ (земля / плавающие компоненты BFS / конфликт источников), покрыт тестами `tests/test_circuit_validator.cpp` (2026-06-15). НО `validateCircuit` **нигде не вызывается** — не подключён к solve-пути / UI. Осталось: вывести предупреждение пользователю при невалидной цепи. |
| A2/V5 — выбор интегратора | `enum IntegrationMethod {BackwardEuler, Trapezoidal}` ЕСТЬ (`CircuitSolver.h`). НО дефолт = BackwardEuler (демпфирует LC ~32%/период), Gear2 нет, осцилляторные демо на Trapezoidal не переключены. |

## ❌ НЕ СДЕЛАНО (проверено: реально отсутствует) — настоящие задачи

| Находка | Объём | Заметка |
|---|---|---|
| V7 / A3 — **AC-источник** `A·sin(2πf·t)` | средний | В коде только DC `VoltageSource` (нет типа AC/частоты/sin). Корневая дыра: выпрямление, AC-демо, осциллограф V(t). Время в `stepTransient` уже есть. |
| A1 — **`LinearSolveResult{status,rank,residual,rcond}`** | средний | Сам линейный солвер решает молча (нет статуса сингулярности/несовместимости). CircuitValidator ловит грубые случаи, но не обусловленность. |
| A2/V5 — Gear2 + дефолт-интегратор для осцилляторных схем | средний | Чинит «звон гаснет слишком быстро». Метод глобальный (`m_liveSim`) — трогать осознанно. |
| B5 — **LIC** поля + **bloom** | крупный | viridis сделан; LIC и Gaussian-bloom (FBO) — нет (см. `docs/MIT_TEAL_RENDERING_PLAN.md`). |
| A3 — Newton-Raphson для нелинейности (Шокли, лампа) | крупный | Сейчас fixed-point 24 прохода. |
| B8 — prediction-вопросы против «ток расходуется»/«батарея=источник тока» | малый-средний | Проверить `src/learning/TaskGenerator.cpp`; целевых вопросов под эти заблуждения, похоже, нет. |
| B3 — библиотека элементов (источник тока, лампа, транзистор, ОУ), save/load, undo/redo, AC-анализ/Боде | крупный | Стратегическое. |
| §8/№12 — карантинный `src/physics/HydraulicSim.cpp` | решение | Судьба файла (ВАРИАНТ Б) не решена — НЕ трогать без решения пользователя. |

## Что было сделано в сессии 2026-06-15 (Партия А + доки)
- Партия А ресёрч-бэклога: оказалась уже в main; добавлены недостающие регрессионные
  тесты (drift, colormap) + абстракция Colormap + мелкая сверка docs↔код. 515 зелёных.
- **Перевод всех 17 английских доков на русский** (3 батча агентов; сеть рвала
  соединения — часть доделана инлайн). Теперь все 24 дока в `docs/` на русском.
- Фикс битого ключа µF + тест.
- Тесты CircuitValidator (был непокрыт). 522 зелёных.
- Этот статус-файл.
